Wildfire dataset folder
=======================

Place the downloaded wildfire CSV here as `wildfire.csv`.

This project also supports loading the dataset from `data/wildfire.csv`.

Use `python setup_wildfire_data.py` to load and preprocess the wildfire data.
